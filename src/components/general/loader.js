import React from 'react'
import PropTypes from 'prop-types'

const Loader = (props) => {
  return (
    <div className="loader">
      <span />
      <span />
      <span />
    </div>
  )
}

export default Loader
